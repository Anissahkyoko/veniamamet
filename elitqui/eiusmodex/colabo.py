# Assuming you're working with Python, here's how you can use the value of `num_return_sequences`:
num_return_sequences = self.num_return_spinbox.value()
print(f"Number of return sequences: {num_return_sequences}")

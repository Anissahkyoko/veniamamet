def process_data(data, _):
    # Process the data without using the second argument
    # ...

# Call the function with two arguments, but the second one is ignored
result = process_data(data, some_value)
